var Chart = React.createClass({
    render: function(){
        return (
            <div className="container">
                lol
            </div>
        )
    }
})

React.render(<Chart />, document.body);